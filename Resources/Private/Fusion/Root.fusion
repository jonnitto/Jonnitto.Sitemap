include: **/*.fusion

root {
    xmlImprovedSitemap {
        condition = ${request.format == 'xml.improvedsitemap'}
        renderer = Jonnitto.Sitemap:XmlSitemap
    }
    textRobots {
        condition = ${request.format == 'txt.robots'}
        renderer = Jonnitto.Sitemap:TxtRobots
    }
}
