prototype(Jonnitto.Sitemap:Priority) < prototype(Neos.Fusion:Tag) {
    tagName = 'priority'
    content = ${q(node).property('xmlSitemapPriority')}
    @if.hasValue = ${!String.isBlank(this.content)}
}
