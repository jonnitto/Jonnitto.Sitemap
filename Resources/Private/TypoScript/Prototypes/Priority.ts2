prototype(XML:Priority) < prototype(Fusion:Tag) {
	@context.priority = ${q(node).property('xmlSitemapPriority')}
	tagName = 'priority'
	content = ${priority}
	@if.hasValue = ${!String.isBlank(priority)}
}
